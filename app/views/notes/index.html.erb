<h1> Notes </h1>
<p><%= link_to "New Note", new_note_path %></p>
<%= link_to "Edit note", edit_note_path(@note) %>
<% @notes.each do |note| %>
  <h2><%= Note.title %></h2>
  <%= link_to "Note page" notes_path %>
  <% end %>

